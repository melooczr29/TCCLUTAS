# ============================================================================
# AMIS Platform - Variáveis de Ambiente (RAIZ - usado pelo docker-compose)
# ----------------------------------------------------------------------------
# Copie este arquivo para `.env` e preencha com valores reais.
#   cp .env.example .env
#
# IMPORTANTE (LGPD/Segurança): o arquivo `.env` real NUNCA deve ser commitado.
# Ele já está listado no .gitignore.
# ============================================================================

# --- PostgreSQL ---
POSTGRES_USER=amis_admin
POSTGRES_PASSWORD=troque_esta_senha_forte
POSTGRES_DB=amis_db
POSTGRES_PORT=5432

# --- pgAdmin ---
PGADMIN_EMAIL=admin@amis.local
PGADMIN_PASSWORD=troque_esta_senha_pgadmin
PGADMIN_PORT=5050
